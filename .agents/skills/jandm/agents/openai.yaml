interface:
  display_name: "Jandm"
  short_description: "Repo-specific patterns and workflows for jandm"
  default_prompt: "Use the jandm repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true